/**
 * Upload limits — must stay in sync with backend Settings:
 * MAX_FILE_SIZE_MB, MAX_ASSETS_PER_RESOURCE
 */
export const MAX_FILE_SIZE_MB = 30
export const MAX_FILE_SIZE_BYTES = MAX_FILE_SIZE_MB * 1024 * 1024
export const MAX_ASSETS_PER_RESOURCE = 5
